name: Workflow No 1
on: workflow_dispatch
jobs:
    first_job:
      runs-on: windows-latest
      steps:
        - name: Initial Step
          run: echo Init Workflow
        - name: Setup Workflow
          run: echo Setup Workflow
        
    
          
