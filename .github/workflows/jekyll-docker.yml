name: Workflow No 1
on: workflow_dispatch
jobs:
    first_job:
      runs-on: windows-latest
      steps:
        - name: Initial Step
          run: console.log('Init Workflow')
        - name: Setup Workflow
          run: console.log('Setup Workflow')
        
    
          
